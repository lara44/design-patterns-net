namespace pattern_decorator.Interfaces
{
    public interface IChicken
    {
        // Interfaz para los objetos que pueden ser decorados
        string GetDescription();
        decimal GetPrice();
    }
}
